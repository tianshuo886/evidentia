"""Synthetic agent test fixtures for Evidentia Tier 1 synthetic integration tests.

ALL outputs from these fixtures are explicitly tagged with:
    "execution_kind": "SIMULATED_FIXTURE"
    "executor": { "kind": "SIMULATED_FIXTURE", ... }

These fixtures are strictly prohibited in production scientific evaluation.
"""
